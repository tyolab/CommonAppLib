<!--
 Sample Admob
 Customize your add layout by overriding the following and add:
      ads:adSize="SMART_BANNER"
      ads:adUnitId="ca-app-pub-xxxxxxxxxxxxxxxxxxxxxxxxx"
-->
<!--
<com.google.android.gms.ads.AdView
      xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:ads="http://schemas.android.com/apk/res-auto"
      android:id="@+id/adView"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:focusable="false"
      android:visibility="visible"
        ads:adSize="SMART_BANNER"
        ads:adUnitId="@string/admob_unit_id_banner1"
      />
-->
<!-- Placeholder carries the @+id/adView so Ad.java (findViewById(R.id.adView)) still compiles
     after the real AdView was removed from this library (4a46e09). Apps that show ads override
     this layout with their own AdView (see app/src/main/res/layout/admob.xml). -->
<View xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/adView"
    android:layout_height="0dp" android:layout_width="0dp">

</View>