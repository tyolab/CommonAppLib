<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="au.com.tyo.app"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!--
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.READ_CONTACTS" />
    -->

    <!-- gradle will merge the manifest files
        android:icon=""
        android:theme="@style/CommonTheme.Light.Pale"
    -->

    <!--
        BE CAREFUL, Android Studio will merge all the  activity definitions
    -->
    <!-- sample common activity definition -->
    <!--
    <activity
        android:name="au.com.tyo.app.CommonActivity"
        android:configChanges="orientation"
        android:label="@string/app_name" >
    </activity>
    -->
    <!--
    <activity
        android:name="au.com.tyo.app.ui.activity.CommonActivityBackgroundProgressogress"
        android:label="@string/app_name" >
    </activity>
    -->

    <application
        android:label="@string/app_name"
        >
        <!-- Add this when you use a google play service library -->
        <!--
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
            -->

        <activity
            android:name="au.com.tyo.app.ui.activity.CommonActivityForm"
            >
        </activity>

        <activity
            android:name="au.com.tyo.app.ui.activity.CommonActivityWebView"
            />

        <activity android:name="au.com.tyo.app.ui.activity.CommonActivityBackgroundProgress"
            />

        <activity android:name="au.com.tyo.app.ui.activity.CommonActivityFileManager"
            android:label="@string/file_manager"
            />

        <activity
            android:name="au.com.tyo.app.ui.activity.CommonActivitySettings"
            android:label="@string/settings" />

        <activity
            android:name="au.com.tyo.app.ui.activity.CommonActivityAbout"
            android:label="@string/about" />
    </application>


</manifest>
